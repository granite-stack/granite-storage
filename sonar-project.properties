# SonarCloud Configuration for Granite Storage
sonar.projectKey=granite-storage

# Project metadata
sonar.projectName=Granite Storage
sonar.projectVersion=1.0.17

sonar.host.url=http://localhost:9000

# Source directories
sonar.sources=src
sonar.tests=tests

# Python version - specify exact version for accurate analysis
sonar.python.version=3.14

# Terraform AWS provider version for accurate analysis
sonar.terraform.provider.aws.version=4.0

# Coverage reports
sonar.python.coverage.reportPaths=coverage.xml

# Exclusions (aligned with coverage omit settings)
sonar.exclusions=\
  **/__pycache__/**,\
  **/*.pyc,\
  **/.pytest_cache/**,\
  **/.mypy_cache/**,\
  **/.ruff_cache/**,\
  .venv/**,\
  htmlcov/**

# Test exclusions
sonar.test.exclusions=tests/**

# Coverage exclusions (aligned with tool.coverage.report)
# sonar.coverage.exclusions=\
#   src/granite_storage/main.py,\
#   src/granite_storage/lambda_main.py,\
#   src/granite_storage/core/telemetry.py,\
#   src/granite_storage/infrastructure/implementations/aws/**

# Encoding
sonar.sourceEncoding=UTF-8
